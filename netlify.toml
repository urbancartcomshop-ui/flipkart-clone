[build]
  publish = "Flipkart"
  command = "echo 'Static site - no build needed' && cp -r ../data Flipkart/ 2>/dev/null || true"

[[redirects]]
  from = "/api/*"
  to = "http://localhost:3000/api/:splat"
  status = 200
